<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grey.myblog.dao.BloggerInfoDAO">

    <resultMap id="BaseResultMap" type="com.grey.myblog.model.dataobject.BloggerInfoDO">
        <id property="id" column="id"/>
        <result property="bloggerName" column="blogger_name"/>
        <result property="avatar" column="avatar"/>
        <result property="intro" column="intro"/>
        <result property="githubUrl" column="github_url"/>
        <result property="email" column="email"/>
        <result property="aboutContent" column="about_content"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, blogger_name, avatar, intro, github_url, email, about_content, update_time, is_deleted
    </sql>

    <!-- 插入博主信息 -->
    <insert id="insert" parameterType="com.grey.myblog.model.dataobject.BloggerInfoDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blogger_info (
            blogger_name, avatar, intro, github_url, email, about_content, update_time, is_deleted
        ) VALUES (
            #{bloggerName}, #{avatar}, #{intro}, #{githubUrl}, #{email}, #{aboutContent}, #{updateTime}, 0
        )
    </insert>

    <!-- 根据ID更新博主信息（动态更新非空字段） -->
    <update id="updateById" parameterType="com.grey.myblog.model.dataobject.BloggerInfoDO">
        UPDATE blogger_info
        <set>
            <if test="bloggerName != null">blogger_name = #{bloggerName},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="intro != null">intro = #{intro},</if>
            <if test="githubUrl != null">github_url = #{githubUrl},</if>
            <if test="email != null">email = #{email},</if>
            <if test="aboutContent != null">about_content = #{aboutContent},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 根据ID逻辑删除博主信息 -->
    <update id="deleteById">
        UPDATE blogger_info SET is_deleted = 1, update_time = NOW() WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 根据ID查询博主信息 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM blogger_info
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 查询第一条博主信息（用于获取唯一配置） -->
    <select id="selectFirst" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM blogger_info
        WHERE is_deleted = 0
        LIMIT 1
    </select>

    <!-- 条件查询博主信息列表 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM blogger_info
        <where>
            is_deleted = 0
            <if test="bloggerInfo.bloggerName != null">AND blogger_name LIKE CONCAT('%', #{bloggerInfo.bloggerName}, '%')</if>
        </where>
        ORDER BY update_time DESC
    </select>

</mapper>
